(()=>{const card=document.querySelector('#adminDaily')?.closest('.card'),box=document.createElement('div');box.className='section';box.style.marginTop='14px';card?.appendChild(box);function render(){const selected=document.querySelector('#dailyHistoryDate')?.value,history=JSON.parse(localStorage.getItem('camellia-daily-history')||'[]'),latest=JSON.parse(localStorage.getItem('camellia-daily-full')||'null'),record=history.find(x=>x.date===selected)||latest;box.innerHTML=`<h3>月経・周期</h3><p>状態：${record?.cycle||'未記録'}<br>最後の月経開始日：${record?.lastMenstrualDate||'未記録'}</p>`}render();document.addEventListener('change',e=>{if(e.target.id==='dailyHistoryDate')render()});addEventListener('storage',render);setInterval(render,1000)})();
